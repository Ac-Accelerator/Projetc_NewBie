#include <algorithm>
#include <iostream>
using namespace std;
void out(char);
struct 
main()
{
    string a;
    cin >> a;
    int l = a.length();
    char c[l];
    for (int i = 0; i < l;i++)
    {
        c[i] = a[i];
    }
    sort(c, c + l);
}
void out(char c[],int l)
{
    for (int i = 0; i < l;i++)
    {
        if
    }
}