#include <iostream>
using namespace std;
main()
{
    int n;
    cin >> n;
    char function;
    string username, userpassword;
    string usernames[100], userpasswords[100];
    for (int i = 0; i < n; i++)
    {
        cin >> function;
        if (function == 'R')
        {
            scanf("%s %s",&username,&userpassword)
            
        }
        else
    }
}